package control_flow

fun main() {
    /**
     * TODO: For (Concepts of ranges), downTo 1 step 2, 0 until 100, iterate over map for example with for-loop
     * Think like foreach
     */
    val ranges: IntRange = 1..10
    val numbers = arrayListOf("1", "2", "3", "4", "5")
    val numbersMap = mapOf("One" to 1, "Two" to 2, "Three" to 3)


    //TODO: While
}